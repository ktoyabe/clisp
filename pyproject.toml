[project]
name = "clisp"
version = "0.1.0"
description = "simple clisp implemented by C language"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
