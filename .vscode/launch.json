{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "c_debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/clisp",
            "args": [
                "${workspaceFolder}/example/sample.lisp"
            ],
            "cwd": "${fileDirname}",
        }
    ]
}